# Tech Stack

<!--
Consumed by: sdd-ticket-start (Step 3, so investigation doesn't guess at what's actually installed)
and sdd-ticket-close (Step 4 — update this file whenever a ticket changes a version it
documents; don't let it go stale).

List what's actually installed and at what version — verified (`composer show`, `npm ls`,
`pip freeze`, etc.), not assumed. Delete this comment block once filled in.
-->

## Language / Runtime

- **Language:** <e.g. PHP 8.3, Node 20, Python 3.12>
- **Framework:** <e.g. Magento 2.4.7, Rails 7.1, Django 5.0>

## Dependency Management

- **Manifest / lockfile:** <e.g. composer.json / composer.lock>
- **Install command:** <e.g. `composer install`, `npm ci`>
- **Drift-check command:** <e.g. `composer install --dry-run`, `npm ci --dry-run`>

## Key Dependencies

| Package | Version | Notes |
|---|---|---|
| <name> | <version> | <why it matters, if non-obvious> |

## Tooling

- **Test runner:** <e.g. PHPUnit, Jest, pytest>
- **Linter / formatter:** <e.g. PHP_CodeSniffer, ESLint + Prettier>
- **CI:** <e.g. GitHub Actions — link to workflow file>

## Infrastructure

- **Hosting / environments:** <e.g. staging, production, and how they map to branches — see branching-strategy.md>
- **Database:** <e.g. MySQL 8.0, PostgreSQL 16>
